<table class='table'>
  <thead class="bg-light">
    <tr>
      <th class="text-center">ジャンル検索</th>
    </tr>
  </thead>
  <tbody>
    <% genres.each do |genre| %>
      <tr>
        <td><%= link_to genre.name, admin_search_genre_path(genre) %></td>
      </tr>
    <% end %>
  </tbody>
</table>